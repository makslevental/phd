{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build notes",
            "command": "pdflatex",
            "args": [
                "index.tex"
            ],
            "type": "shell",
            "options": {
                "cwd": "${fileDirname}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}